server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/doctore/Spring5Microservices_ConfigServerData
        native:
          searchLocations: file:///${GIT_REPO}
